@code {
    // Home page is now served by a Razor Page (Index.cshtml) for static server-side rendering.
    // This component remains as a placeholder so localization resources tied to
    // community.Pages.GameListPage.Home can still be referenced where needed.
}
